# {{Language}} — Mechanics Analysis: {{Mechanism Name}}

**Slang type:** {{one of `00_Usage_Tier_Taxonomy.md`'s derivation types, e.g. `taboo_inversion`,
`semantic_shift`, or a dot-notation subcategory}}
**Derived from tiers:** {{which standard-language usage tier(s) this mechanism draws from, e.g.
`technical`, `taboo.religious`}}

---

## What the mechanism is

{{Plain-language explanation of how this slang-formation mechanism works, naming the real
linguistic phenomenon — don't invent terminology that already exists in the linguistics
literature.}}

## Examples

| Term | Explanation | Corpus Reference | Attested Era | Attested Region | Source Type | Transcription Confidence |
|---|---|---|---|---|---|---|
| *(term)* | *(how this example demonstrates the mechanism)* | *(pointer into `language_corpus/{{Language}}/`)* | *(or "—")* | *(or "—")* | `dictionary`/`forum`/`subtitle_ai_generated`/etc. | required if source_type is a subtitle/transcript type — `verified`/`plausible_unverified`/`low_confidence` |

**Transcription risk carries through.** If this mechanism's only supporting example is a
`low_confidence` or `plausible_unverified` transcript-derived entry, say so explicitly in the
writeup below — don't present it as settled. See
`../../../language_corpus/00_Source_Reliability_Guide.md`.

## Writeup

{{Narrative analysis — quote real examples, connect to `established/` entries where relevant, cite
sources. Same style as the reference extraction writeups: ground every claim in the actual source
material, flag genuine uncertainty rather than smoothing over it.}}
